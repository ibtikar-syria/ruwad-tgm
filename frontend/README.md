# Frontend (Admin UI)

Vite + React admin for the Telegram Group Manager.

```bash
npm install
npm run dev
```

Proxies `/api` to the Worker at `http://127.0.0.1:8787`. See the [root README](../README.md).
